# Independent review transcript: change ebfc501..549b2e8 (360-audit batch 2)

Reviewer: `deepseek/deepseek-v4-pro` through OpenRouter, a model that wrote none of this change and had no tools. It received the brief, the acceptance criteria, the measured evidence, the 27-gate sweep, the diffstat, every new test class name and the complete code diff for `pmos/`, `tools/` and `lint.py`. The document and test bodies were not sent and the brief said to judge those only through the diffstat.

**Four rounds. The first three returned `reject`, and this record keeps all of them.** Rounds 1 and 2 raised four P0 findings; the maintainer's session refuted every one with evidence (they described `pmos/hooks.py`, which is not in this diff; misquoted the CHANGELOG; and contradicted a shipped passing test). Round 3, with reasoning enabled, raised a REAL fail-open in F11's NFR column binding, which was fixed before round 4 rather than argued with. Round 4 accepts.

**What round 3 found, and what the fix exposed.** `_column(header, *words)` bound the first column whose header contained a keyword, so a decoy `Expected Waiver ID` column bound instead of the real one. On execution it was worse than reported: a requirement row reading `Met` with its verification evidence deleted produced NO finding at all, which is the defect F11 exists to catch. Fixing it also exposed that the duplicate-waiver-ID refusal had never fired once, because it queried a map keyed by NFR ID for a `WV-` key. The binding now goes through the same `_bind_columns` helper F02 uses: exactly one exactly-named header binds, more than one is refused by name, and a table where nothing binds is refused. Eight tests were added, each red under its own guard's reversion.

Dashes the replies wrote as literal characters appear here as JSON `\u2014` escapes, which decode to the same text, because this repository's lint refuses a literal em dash in markdown.

## Round b360-1 (reasoning off; rejected, all P0s later refuted)

- model resolved: `deepseek/deepseek-v4-pro`, provider: DigitalOcean
- tokens: 22697 prompt, 1229 completion; cost $0.02626182; 29 s
- verdict: **reject**

```json
{
  "verdict": "reject",
  "checked": [
    "A1: sed script scanner in lint.py _redirects_execution does not parse short-option bundles, leading to a claim gap",
    "A2: constructed 'git -c diff.external=rm --no-pager diff' where the --no-pager abbreviation resolves within the git subcommand dispatch bypassing the allowlist",
    "A4: CHANGELOG says F15 closes \"budget >= commercial floor\" contradiction; diff removes that guard and renames the section, so the closure claim is false",
    "A1: example-availability generator parses domain card relations; verified it catches the stale text scenario described in A3"
  ],
  "issues": [
    {
      "severity": "P0",
      "problem": "The sed command scanner in lint.py fails closed on a legitimate POSIX invocation and fails OPEN on an abbreviated GNU long option. The scanner permits `sed -n` (bundled short option) as a write, but rejects `sed -n` as a standalone option because it only matches `--<opt>=<arg>` and `-<opt> <arg>` patterns, missing the bundled abbreviation case entirely.",
      "counterexample": "`sed -n ' /pattern/ w /tmp/out ' input` \u2013 the scanner sees `-n` as a standalone short flag with no value, does not classify it as a write option, and returns allow, but the actual `sed` invocation writes to `/tmp/out`.",
      "fix": "The write-option scanner must detect short-option bundles that contain write flags. For `sed`, `-n` bundled with `i`, `a`, `c`, `w`, or `r` should be classified as a write. Alternatively, the scanner should fail closed (ask) when it encounters an unrecognized option bundle for a known write-capable command."
    },
    {
      "severity": "P0",
      "problem": "The git subcommand dispatch in `_command_redirects` fails OPEN on GNU long-option abbreviation. The policy rejects `git -C <path>` by classifying -C as a redirect option, but `git --no-pager diff` where `--no-pager` is an abbreviable long option can match a different pattern if the abbreviation algorithm is not implemented.",
      "counterexample": "`git -c diff.external=rm --no-pager diff` \u2013 if the allowlist fails to recognize `--no-pager` as an abbreviation of a known safe/unsafe option, the entire argument may be passed through without blocking the `-c` config injection that enables execution.",
      "fix": "The git subcommand scanner must implement GNU long-option abbreviation matching against the known option vocabulary, and any abbreviation that is ambiguous or unrecognized must fail closed (ask)."
    },
    {
      "severity": "P0",
      "problem": "F15 closure claim is false. The CHANGELOG states \"F15 budget-stop contradiction: the budget section no longer claims a replacement budget is a downgrade\", but the diff at docs/ARCHITECTURE.md and the pricing-packaging template shows the budget guard is removed entirely and the section is renamed, not fixed. The stop-cost contradiction described in F15 (budget >= commercial floor) is not addressed; there is no new guard or check that enforces budget-floor consistency.",
      "counterexample": "A one-pager can now set a stop cost of $50k and a commercial floor of $75k without any check catching the contradiction, because the guard that used to flag it is deleted and nothing replaces it.",
      "fix": "Restore or replace the budget-stop contradiction check. The check should verify that the stop cost is >= the commercial floor estimate, or that the one-pager explains the discrepancy."
    },
    {
      "severity": "P1",
      "problem": "The evidence-notes contract check in docs_contract.py skips detection of a quantitative note that contains a verbatim quote. The check looks for `> \"` anywhere in the claim block, but a user could embed the quote inside a field value (e.g., `- **Calculation:** \"average of 3 runs\"`) where the check's line-based regex does not match.",
      "counterexample": "A quantitative evidence note with `- **Calculation:** \"average of 3 runs\"` passes the check because the quote is on a field line, not a blockquote line, yet it violates the rule that a quantitative note should not quote a sentence nobody said.",
      "fix": "The `QUOTE_LINE` regex should be replaced with a search over the entire claim body for quoted strings, not just lines beginning with `> \"`. For example, `re.finditer(r'\"[^\"]+\"', body)` to catch embedded quotes."
    },
    {
      "severity": "P1",
      "problem": "The pricing contract check for section 8 change-record fields uses `cell.startswith(field)` to match column headings. A heading like `Pays after (projected)` will match `Pays after`, but a heading like `Projected pay after` will not. This is a known limitation stated in the comment, but it means the check can be bypassed by a synonym.",
      "counterexample": "A user renames the column from `Pays after` to `Post-change payment` \u2013 the check fails to find the field and reports an error, but the template still contains the required information semantically.",
      "fix": "No fix required if the acceptance criteria only demand the columns *as named*, but if the acceptance criteria demand the *information*, the check should use a synonym-aware mapping similar to `_bind_columns` in lint.py."
    }
  ]
}
```

## Round b360-2 (reasoning off, scope stated; rejected, P0 refuted)

- model resolved: `deepseek/deepseek-v4-pro`, provider: DigitalOcean
- tokens: 22856 prompt, 1187 completion; cost $0.02634012; 28 s
- verdict: **reject**

```json
{
  "verdict": "reject",
  "checked": [
    "F02 UAT acceptance check logic in lint.py for defect records and exception registers",
    "F11 NFR closure check logic in lint.py for ID format and revision matching",
    "F12 evidence note contract logic in tools/docs_contract.py",
    "F16 pricing transition contract logic in tools/docs_contract.py",
    "A2: trace a command that the F01 policy might allow, noting the diff does NOT contain the shell policy",
    "A4: contradiction between code diff behavior and CHANGELOG claims",
    "Example-availability gate (F07) via what_checks.py and its generated claims"
  ],
  "issues": [
    {
      "severity": "P0",
      "problem": "Defects in the UAT acceptance check logic within lint.py cause it to silently accept a document that claims completion but has failed charters with no defect record and no approved exception, violating the exact contract it was written to enforce.",
      "counterexample": "A UAT plan document with a charter that has Result = Fail, no defect ID (cell is blank or 'none'), and no exception ID (cell is blank or 'none'). The check `_charter_findings` fires two add() calls\u2014one for missing defect, one for missing exception\u2014but after the second add(), it hits `return` without reaching any of the checks that would reject the plan on this basis. Meanwhile, `claims_uat_complete()` returns True because a `Signed:` line exists at end of document. The result is that `uat_problems()` returns a list of findings, but these findings are merely informational; there is no line in `_charter_findings` that actually FAILS the plan or corrupts the exit gate. The finding tells the user the charter is bad, but the tool does not block the claim\u2014it permits a 'Fail' charter with no defect and no exception to coexist with a sign-off, defeating the purpose of the acceptance criteria.",
      "fix": "In `_charter_findings()`, after determining that Result is Fail, ensure that a missing or absent defect AND a missing or absent exception (i.e., no acceptable pair) produce a fatal condition\u2014not a soft warning\u2014that prevents the workspace check from passing. Alternatively, lift the gate logic so that `uat_problems` returns a special error code that the workspace check treats as a hard fail, not just a lint finding."
    },
    {
      "severity": "P1",
      "problem": "The NFR closure check in lint.py fails to recognize a valid waiver ID if it appears embedded in prose adjacent to the status column, leading to a false positive defect.",
      "counterexample": "A row with Status = 'Waived (WV-01)' and the waivers section containing that waiver. `STATUS_WAIVED_RE` matches 'waived', so the check proceeds to `WAIVER_ID_RE.findall(status)`, which extracts the waiver ID. However, if the waiver data is stored in the Verified By column rather than Status as the template now defines, the check may incorrectly report that no waiver exists against the requirement when one does.",
      "fix": "The waiver association logic must also examine the Verified By field for waiver IDs when the status implies a waiver, or the template specification must be tightened to disambiguate which column holds the ID. "
    },
    {
      "severity": "P1",
      "problem": "The evidence note contract check in tools/docs_contract.py permits a filled quantitative note to carry a verbatim quote without flagging it, if the quote block appears on a line that matches QUOTE_LINE but the kind declared is not 'text quotation'. The logic tests `if stated != \"text quotation\" and quoted is not None:` but `QUOTE_LINE` matches any line starting with `> \"`, which can occur inside a quantitative block's description.",
      "counterexample": "A filled sahulat-evidence-note.md whose Evidence Kind is 'quantitative' and whose 'Calculation' field contains a blockquoted example like `> \"We take the 90th percentile latency\"`. The quoted line triggers `quoted is not None`, and `stated` is 'quantitative', so the check fires an error: 'a quantitative note quotes a sentence nobody said'. This is a false positive if the quote is merely a description of the calculation, not a verbatim sentence from a human source.",
      "fix": "Restrict QUOTE_LINE detection to only the lines immediately below the 'Where in the source' field label, or require that the quote match does not appear inside another evidence kind block. Alternatively, move the quote detection to only activate when the declared kind is 'text quotation'."
    },
    {
      "severity": "P3",
      "problem": "The pricing contract check in tools/docs_contract.py incorrectly reports that the unit economics worksheet does not name the section it feeds if the text `section 3a` appears but is inside an HTML comment or code fence, because `_shown()` strips comments already but the test only checks for the plain string.",
      "counterexample": "If `frameworks/metrics/unit-economics.md` contains `<!-- feeds section 3a -->` but no visible occurrence, `_shown()` removes it, and the check fails. This is not a defect\u2014it's correct behavior\u2014but the tool's behavior could be surprising if the author expects HTML comments to count.",
      "fix": "No fix required; the behavior is correct as-is because comments are not visible content."
    }
  ]
}
```

## Round b360-3-deep (reasoning enabled; rejected over a real fail-open, now fixed)

- model resolved: `deepseek/deepseek-v4-pro`, provider: DigitalOcean
- tokens: 22935 prompt, 28504 completion; cost $0.083460492; 565 s
- verdict: **reject**

```json
{
  "verdict": "reject",
  "checked": [
    "A1 UAT column binding: traced _bind_columns \u2013 multiple matches forces None and reports error, missing column leads to error; fails closed.",
    "A1 NFR column finding: traced _column \u2013 substring match on headers can misbind (e.g. 'Expected Waiver ID' matches 'waiver id'), allowing the check to read wrong column and miss a required empty field (fail-open risk).",
    "A1 pricing contract guard: verified check_pricing_contract uses exact heading string and exact substring presence for terms/routes; absence triggers error (fail-closed).",
    "A2 no new execution/write paths: the diff adds workspace lint checks (UAT, NFR) and doc contract checks; it does not touch pmos/hooks.py or the shell-command policy, so F01 bypasses are not introduced by this change.",
    "A4 code vs CHANGELOG alignment: UAT acceptance check matches F02, NFR closure matches F11, evidence contract matches F12, pricing contract matches F16; no contradiction observed with the brief's description of each finding."
  ],
  "issues": [
    {
      "severity": "P1",
      "problem": "The NFR closure check uses _column(header, *words) which returns the first column whose header contains any of the words as a substring. This can misbind when another column's header includes those words (e.g. 'Expected Waiver ID' matches 'waiver id'), causing the check to read placeholder/incorrect data and fail to detect a missing actual required column (fail-open). The same risk applies to the verified and status columns in requirement tables.",
      "counterexample": "A waiver table with headers [Expected Waiver ID, Waiver ID, NFR ID]. _column(header, 'waiver id') returns index 0 ('Expected Waiver ID'). If the user fills that cell with 'WV-01' and leaves the real 'Waiver ID' cell empty, the check sees a valid waiver ID, does not flag the missing one, and the row passes validation.",
      "fix": "Replace _column with name-based binding similar to _bind_columns in the UAT check: define accepted header names per column, require exactly one match, and refuse ambiguous tables. Apply to waiver ID, NFR ID, verified and status columns."
    },
    {
      "severity": "P2",
      "problem": "The NFR closure check maps waivers to requirements using waived_by.setdefault(ids[0], wid), storing only the first waiver ID per NFR ID. If multiple waivers exist for the same requirement and a requirement row references a later one, the check produces a false mismatch.",
      "counterexample": "Two waiver rows: WV-01 -> NFR-01, WV-02 -> NFR-01. Requirement row status: 'Waived (WV-02)'. waived_by['NFR-01'] is 'WV-01'; the check reports 'NFR-01 says it is waived under WV-02 and the waivers section records WV-01 against it', which is incorrect.",
      "fix": "Store a set of waiver IDs per NFR ID (e.g., waived_by.setdefault(ids[0], set()).add(wid)), and accept the row if any named waiver belongs to that set. Alternatively, flag duplicate NFR IDs in the waivers section as an error."
    }
  ]
}
```

## Round b360-4-deep (reasoning enabled, on the fixed tree; accepted)

- model resolved: `deepseek/deepseek-v4-pro`, provider: DigitalOcean
- tokens: 24189 prompt, 10232 completion; cost $0.046617732; 219 s
- verdict: **accept**

```json
{
  "verdict": "accept",
  "checked": [
    "UAT _bind_columns: traced a charter table with two columns whose headers both match the 'result' names (e.g., 'Result' and 'Outcome'); the check reports ambiguity, binds None, and refuses to read either silently \u2014 fail-closed.",
    "NFR waiver mapping: verified that a document listing two waivers for the same requirement, plus a requirement row whose status cell names only the first, correctly records both in waived_by via setdefault/append and reports a mismatch on the intersection check \u2014 fail-closed.",
    "NFR column binding fix: confirmed that the _bind_columns helper matches exact header names from the column spec; a column headed 'Expected Waiver ID' does not bind because it is not in the accepted names, and the table is refused (missing columns reported or skipped) \u2014 closes the previous fail-open.",
    "UAT _charter_shape: tested a table with four charter columns (charter number, AC, tester, build) but a result column named 'Final outcome' (not in UAT_COLUMNS). _charter_shape detects it via the column count, reports it as a charter table under another heading, and lists the unreadable column \u2014 no silent pass.",
    "Evidence note contract: examined check_evidence_contract on a filled note declaring 'observation' but missing the 'What was observed' field; the check errors on the missing field \u2014 fail-closed on incomplete notes."
  ],
  "issues": [
    {
      "severity": "P3",
      "problem": "NFR closure reads cell values with bare `strip()` rather than stripping markdown emphasis (asterisks, underscores). A waiver ID cell written as `*WV-01*` or `_WV-01_` (common in template examples) fails `WAIVER_ID_RE.match` and produces a false positive 'waiver row has no waiver ID'.",
      "counterexample": "A waiver table cell containing only `*WV-01*` (italics). `_cell` returns `'*WV-01*'`, the regex does not match, and the check reports the row as having no waiver ID, even though the ID is present and human readers recognize it.",
      "fix": "Apply `_plain()` (or a similar emphasis-stripping helper already used by the UAT check) to `_cell` results before matching against `WAIVER_ID_RE`, `NFR_ID_RE`, and `NFR_ROW_ID_RE` to make the check robust against markdown formatting."
    }
  ]
}
```
